import { StyleSheet } from 'react-native';
import { THEME_COLOR } from '../../screens/Home/';

export const styles = StyleSheet.create({
  container_header: {
    width: '100%',
    height: 80,
    backgroundColor: THEME_COLOR,
    justifyContent: 'center',
    alignItems: 'center',
  },
});
